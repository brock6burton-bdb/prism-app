import type {DecisionResult} from "@/types/decision";
export function DecisionResultView({result,onRestart}:{result:DecisionResult;onRestart:()=>void}){const runner=result.alternatives[0];return <main className="resultPage"><section className="heroCard"><span className="eyebrow">Your decision strategy</span><h1>{result.headline}</h1><p>{result.summary}</p><div className="meta"><div><span>Strategy fit</span><strong>{result.winner.score}/100</strong></div><div><span>Next-best path</span><strong>{runner.label}</strong></div>{result.estimatedSavings&&<div><span>Potential savings</span><strong>{result.estimatedSavings}</strong></div>}</div></section><section className="grid"><article className="card wide"><span className="label">Why this fits your situation</span><div className="reasons">{result.reasons.map((r,i)=><div key={r}><b>{i+1}</b><p>{r}</p></div>)}</div></article><article className="card"><span className="label">Why alternatives ranked lower</span>{result.whyNot.map(x=><details key={x.id}><summary>{x.title}</summary><p>{x.explanation}</p></details>)}</article><article className="card"><span className="label">Your first three steps</span><ol>{result.actionPlan.map(x=><li key={x}>{x}</li>)}</ol></article><article className="card stop"><span className="label">Know when to stop</span><ul>{result.stopConditions.map(x=><li key={x}>{x}</li>)}</ul></article></section><div className="resultActions"><button className="secondary" onClick={onRestart}>Start over</button><button className="primary" onClick={()=>window.print()}>Save this strategy</button></div></main>}
